# Project Codex Memory

Use the global skill at `C:/Users/JasonGabrielGuijo/.codex/skills/lean-unified/SKILL.md` by default in this repository.

Default behavior:
- Operate as one unified full-stack agent.
- Keep token usage low by default, but spend more when correctness, safety, debugging, migrations, security, or ambiguity require it.
- Prefer small focused patches and existing project patterns.
- Treat `https://github.com/jasonguijo-vienovo/vienovo-forms.git` as the canonical/original repository for this project unless the user explicitly says otherwise.
- Treat repository docs as project memory before inventing new structure.
- Do not spawn parallel subagents unless the user explicitly asks for multi-agent work in the current turn.
- Verify with the cheapest useful checks first.
- If the skill is not formally registered in the current session skill list, still follow the `lean-unified` workflow manually for this repo.

Call name:
- `lean-unified`
